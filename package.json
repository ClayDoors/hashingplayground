{
  "name": "hashingplayground",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "npm install && mkdir -p uploads && mkdir -p /mydir",
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1",
    "express-zip": "^3.0.0",
    "multer": "^1.4.5-lts.1",
    "body-parser": "^1.20.0"  
  }
}
